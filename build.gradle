// ChatApp/build.gradle
allprojects {
    group = 'org.example' //TODO: find good name
    version = '1.0'
}

subprojects {
    apply plugin: 'java'
    // Add any common configurations for subprojects here
}
